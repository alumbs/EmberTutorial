{
  "name": "nodeServer",
  "version": "0.0.0",
  "description": "trying to serve out the ember site as a static site in node",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Chibuzor Alumba",
  "license": "ISC",
  "dependencies": {
    "ecstatic": "^0.5.3",
    "restify": "^2.8.1"
  }
}
